# Caesar Predict V2
Live football fixtures with API-Football predictions, league filters, search, confidence bands, recent form, H2H and goal analysis.
Render: Root Directory blank; Build Command `npm install`; Start Command `npm start`; Environment Variable `API_FOOTBALL_KEY` = your private key.
